openapi: 3.0.0
info:
  title: Reference Params
  version: 1.0.0
paths:
  /items:
    get:
      operationId: listItems
      parameters:
        - $ref: '#/components/parameters/limitParam'
      responses:
        '200':
          description: OK
components:
  parameters:
    limitParam:
      name: limit
      in: query
      required: false
      schema:
        type: integer
